# rapid-mlx 0.10.x TODO — top-tier for the 3 Mac families

Derived from 4-agent parallel research (Qwen 3.6 + Gemma 4 + gpt-oss externals + rapid-mlx internal audit) on 2026-07-04, restructured 2026-07-04 per operator directive into a **versioned patch-release cadence** rather than a single monolithic release. Scope pivoted from the earlier quantization-first plan (SpinQuant / W8A8 / QuIP#) → **top-tier support for the three model families that Mac users actually run**. Quant axis moved to `0.11-TODO.md`.

> **Read me first — the big finding.** Qwen 3.6 has a **native MTP head baked into every checkpoint** (27B + 35B-A3B), sharing the embedding + `lm_head` weights with the main model. No external drafter needed. Our `0.9.x` profile note "no MTP/drafter trained" is **wrong** and needs correcting in 0.10.3. mlx-community already mirrors `Qwen3.6-27B-MTP-4bit` and `Qwen3.6-35B-A3B-MTP-4bit` — the assets are ready. Reference in-the-wild speedup: **2.24× decode on 27B** (`youssofal/MTPLX`, MLX-native).

> **Read me second — Gemma 4 KV-sharing is missing.** Google published assistant sidecars for **all 5 sizes** (E2B, E4B, 12B, 26B-A4B, 31B) with KV-sharing borrowed from Gemma 3n — the drafter reuses the target's KV cache so it skips prefill. Our 0.9.13 shipped 12B only, and does NOT use KV-sharing. Every turn pays for drafter prefill. Fixing this is the biggest single perf win of 0.10 — lands in 0.10.2.

> **Read me third — gpt-oss EAGLE-3 is deferred, not shipped.** Amazon / RedHat / NVIDIA published EAGLE-3 drafters for gpt-oss-20b and 120b, but ALL are CUDA / vLLM-only. MLX port = weeks of greenfield kernel work with no upstream precedent. 0.10 gpt-oss scope is aliases + `reasoning_effort` passthrough + harmony parser hardening. EAGLE-3 goes on the 0.11+ watchlist.

> **Read me fourth — the SOTA vs adhoc gate.** Every Layer B (perf) item on this file must be paper-backed (arxiv ID or major-lab publication) AND lossless-preserving. No community "special quants" without formal treatment. No layer-skip / early-exit heuristics without paper. No "found on Twitter" claims. The list in each version below has been (or will be — see research pending) filtered against this gate. See `## Perf technique SOTA gate` at the bottom.

---

## Version cadence & release-notes hero per version

| Version | Focus | Effort | Release hero |
|---|---|---|---|
| **0.10.0** | Layer A across all 3 families + rapidmlx.com mirror updates + tiny gpt-oss perf knob | ~2 weeks | **"gpt-oss coverage full"** — short alias, 4/8-bit, 120B tier, `reasoning_effort` |
| **0.10.1** | Layer C — Tier-1 agent integration (codex-cli + claude-code + opencode + qwen-code + openhands) | ~1-2 weeks | **"Verified with the agent stack you actually use"** — integration matrix + qwen-code narrative post |
| **0.10.2** | Layer B for **Gemma 4** — KV-share (A1) + Google MTP sidecars all 5 sizes + fused qSDPA kernel (A2) + TurboQuant K4V4 (B2, gated on A2) | ~3-4 weeks | **"Gemma 4: 2× prefill/TTFT via KV-sharing"** — plus MTP across every size (E2B → 31B) |
| **0.10.3** | Layer B for **Qwen 3.6** — FastMTP head (A3) + native MTP wiring + DeltaNet fp32 fix (B3) + Phase 4 plumbing incl. `mx.compile` bucketed (A5) | ~3 weeks | **"Qwen 3.6 MTP works out of the box"** — 2.24× baseline + FastMTP layer (+82% on top of vanilla MTP) |
| **0.10.4** (candidate) | gpt-oss perf focus — Fused grouped-GEMM MoE + Speculative MoE (A4). NOT COMMITTED — promote only if 0.10.3 finishes healthy | ~2-3 weeks | **"gpt-oss gets first-class perf"** — matches Gemma 4 / Qwen 3.6 story |
| **Total** | | **~9-14 weeks** wall-clock (4 committed patch releases + 1 candidate) | |

---

## 0.10.0 Release acceptance — Layer A + tiny extras

Every alias registered + rapidmlx.com mirror in sync + gpt-oss `reasoning_effort` accepted.

### Qwen 3.6 — Layer A (~1 day)

- [ ] Add `qwen3.6-27b-mtp-4bit` → `mlx-community/Qwen3.6-27B-MTP-4bit` (asset for 0.10.3 native MTP; land alias first)
- [ ] Add `qwen3.6-35b-mtp-4bit` → `mlx-community/Qwen3.6-35B-A3B-MTP-4bit`
- [ ] Add `qwen3.6-27b-optiq-4bit` → `mlx-community/Qwen3.6-27B-OptiQ-4bit` (mixed-precision, sensitive layers @ 8-bit)
- [ ] Add `qwen3.6-35b-optiq-4bit` → `mlx-community/Qwen3.6-35B-A3B-OptiQ-4bit`
- [ ] Add `qwen3.6-27b-ud-{3bit,6bit}` from `unsloth/Qwen3.6-27B-UD-MLX-{3bit,6bit}` (3bit unlocks 16GB Mac fit)
- [ ] Add short aliases: `qwen3.6-27b` → default 4bit, `qwen3.6-35b` → default 4bit
- [ ] **Delete** from profile: any 4B / 9B / 122B-A10B Qwen 3.6 aliases (those are 3.5, not 3.6)
- [ ] Rapid-MLX issue #346 (UD-variants + 8-bit gap) closed by this task

Edit target: `vllm_mlx/aliases.json` + `vllm_mlx/model_aliases.py:90-212`.

### Gemma 4 — Layer A (~1 day)

- [ ] Mirror `mlx-community/gemma-4-{e2b,e4b,26B-A4B,31B}-it-assistant-bf16` as `gemma-4-{e2b,e4b,26b,31b}-assistant` aliases (required for 0.10.2 MTP)
- [ ] Add `gemma-4-12b-optiq-4bit` → `mlx-community/gemma-4-12B-it-OptiQ-4bit` (+6.40 Capability Score vs uniform 4-bit)
- [ ] Add `gemma-4-e4b-optiq-4bit`
- [ ] Add `gemma-4-e2b-{6bit,8bit}`, `gemma-4-e4b-{6bit,8bit}` — mlx-community has full quant ladder
- [ ] Add `gemma-4-26b-8bit` (M3 Ultra 96GB sweet spot)
- [ ] Mirror `mlx-community/gemma-4-mtp-qat` collection (QAT + MTP combined)
- [ ] Flag `diffusion-gemma-26b-*` aliases as `experimental: true` in profile

### gpt-oss — Layer A + tiny Layer B (~1 day)

- [ ] Add short `gpt-oss-20b` → default to `mlx-community/gpt-oss-20b-MXFP4-Q8`
- [ ] Add `gpt-oss-20b-mxfp4-q4` → `mlx-community/gpt-oss-20b-MXFP4-Q4` (16GB Mac fit)
- [ ] Add `gpt-oss-20b-4bit` → `mlx-community/gpt-oss-20b-OptiQ-4bit` or `NexaAI/gpt-oss-20b-MLX-4bit`
- [ ] Add `gpt-oss-20b-8bit` → `mlx-community/gpt-oss-20b-mlx-q8` or `lmstudio-community/gpt-oss-20b-MLX-8bit`
- [ ] Add `gpt-oss-120b`, `-mxfp4-q4`, `-mxfp4-q8`, `-4bit` (fit budget: 96GB/192GB/256GB per siliconscore)
- [ ] Add `gpt-oss-safeguard-20b` if MLX quant published — Oct 2025 safety classifier, same harmony format
- [ ] **[Layer B tiny]** `reasoning_effort: low|medium|high` request-param passthrough, injected into system prompt per harmony spec. Effort: ~2 hours. This is the only real user-facing perf knob for gpt-oss (EAGLE-3 deferred), so it lands with the aliases in 0.10.0 rather than waiting.
- [ ] Set `is_moe: true` on all gpt-oss profiles

### rapidmlx.com mirror updates (~1 day)

- [ ] Sync every new alias to the rapidmlx.com model catalog (docs.rapidmlx.com model pages + landing page featured-models block)
- [ ] Update the "Popular models on Mac" section with current standings (Qwen 3.6 27B / 35B-A3B, Gemma 4 12B / 26B, gpt-oss 20B / 120B)
- [ ] Verify every `-MTP-4bit` mirror actually exists in mlx-community BEFORE registering (audit finding: some 0.9 aliases point to repos that later 404-ed)
- [ ] Publish rapidmlx.com blog post announcing 0.10.0 alias coverage

---

## 0.10.1 Release acceptance — Layer C agent integration

### The top-tier agent list (finalized 2026-07-04 from research `aa64fe38e1d6d0575`)

**Tier 1 — MUST support in 0.10.1** (every Tier-1 × 3-family cell must have verified integration + regression fixture before tag):

| Agent | Stars / momentum | Why Tier 1 |
|---|---|---|
| **codex-cli** (`openai/codex`) | 95K, +35-40K in 90d, ~5M WAU | Half of the Willison/Lambert daily-driver duopoly |
| **claude-code** (`anthropics/claude-code`) | 136K, +21K in 90d | Other half of the duopoly. Verify our `openclaude` profile targets the CURRENT wire format, not the pre-source-available spec |
| **opencode** (`sst/opencode` / anomalyco) | 182K, +40-50K in 90d, 7.3× Roo Code | **Terminal-native, local-model-focused** — single best fit for a Mac MLX server |
| **qwen-code** (`QwenLM/qwen-code`) | 25.7K, +5K in 90d, daily commits | Native OpenAI-compat with `baseUrl` config for localhost:port/v1 — plugs into rapid-mlx for free. **NOT in our current 12 — add** |
| **openhands** (`All-Hands-AI/OpenHands`) | 79K, +11K in 90d | 1.0 on new Software Agent SDK; strong sandbox/CI angle |

**Tier 2 — keep profile, low investment** (touch only if a Tier-1 bug fix accidentally touches these):

| Agent | Notes |
|---|---|
| **cline** | 64K, +6K/90d, steady; standalone CLI shipped |
| **pydantic-ai** | 21.2M PyPI/mo, v2.5.0 shipped 2026-07-04, fastest-rising framework; MindsDB migrated to it |
| **continue.dev** | 34.7K, IDE-first alternative. **NOT in our current 12 — add** if cheap |
| **smolagents** | 592K/mo, niche 1000-LOC minimalist framework |
| **aider** | 47K stars but last commit 2026-05-22 — slipping; keep on life support |

**Tier 3 — kill or downgrade from current 12**:

| Agent | Action | Why |
|---|---|---|
| **langchain** | **Rename profile to `langgraph`** | LangChain still 315M PyPI/mo but HN sentiment durably negative (CVE-2025-68664 reinforced); LangGraph (61M/mo, v1.2.7, Klarna/Uber/LinkedIn/Elastic in prod) is where enterprise went |
| **hermes** | **Archive** | Not a framework — it's Nous Research's model family + a Feb 2026 "Hermes Agent" project with no meaningful PyPI presence. My 0.9-era profile addition was a mis-classification |
| **goose** | **Downgrade to Tier 2 / doc-only** | Handed to Linux Foundation H1 2026; repositioned as general automation, not coding-first |

**New entrants MISSING from our 12**:

| Agent | Priority |
|---|---|
| **qwen-code** | Tier 1 add (see above) |
| **kilo-code** (`Kilo-Org/kilocode`) | Tier 2 add — 19.8K → 25.5K in ~30 days, Cline fork |
| **langgraph** | Replace `langchain` slot |
| **continue.dev** | Tier 2 add |

**Surprises worth surfacing to the operator**:
- **OpenAI-compat is the free lunch**: qwen-code, opencode, cline, codex, claude-code all speak OpenAI wire. rapid-mlx already serves that endpoint — most Tier-1 integration is **profile presets + docs**, not protocol work.
- **Alibaba banned Claude Code internally 2026-07-03** ([US News](https://www.usnews.com/news/top-news/articles/2026-07-03/alibaba-to-ban-claude-code-in-workplace-over-alleged-backdoor-risks-source-says)) over alleged backdoor risk. Narrative upside for the **qwen-code + local-MLX** story. Consider a docs.rapidmlx.com "run Qwen 3.6 with qwen-code, locally" tutorial post as part of 0.10.1.
- **Prominent-dev cohort has collapsed to CLIs, not frameworks** — Willison, Lambert, Mollick, Karpathy all cite Claude Code / Codex; nobody publicly names LangGraph or pydantic-ai as their daily driver. Weights our investment away from framework glue and toward CLI presets.
- **Roo Code archived May 2026** — if the profile is anywhere in our tree, drop it.

### 0.10.1 concrete work items

**Portfolio ops (~1 day)**:

- [ ] Add profile: `agents/profiles/qwen-code.yaml` (native OpenAI-compat baseUrl config, Tier 1)
- [ ] Add profile: `agents/profiles/langgraph.yaml` (Tier 3 replacement for LangChain)
- [ ] Add profile: `agents/profiles/kilo-code.yaml` (Tier 2)
- [ ] Add profile: `agents/profiles/continue-dev.yaml` (Tier 2)
- [ ] **Archive**: `agents/profiles/hermes.yaml` (mis-classification), `agents/profiles/langchain.yaml` (replaced), `agents/profiles/goose.yaml` (downgraded)
- [ ] Verify `agents/profiles/openclaude.yaml` targets CURRENT Claude Code CLI wire format (source-available since March leak) — audit for stale spec references
- [ ] Rebuild the family × agent matrix planned for 0.10.3 Phase 4 plumbing off this NEW Tier-1 list (5 agents × 3 families = 15 cells to verify)

**Confirmed integration bugs (~1 week total)** — these ship in 0.10.1 regardless of the Tier-1 list:

- [ ] **Qwen 3.6 · Default `preserve_thinking=true`** for Qwen 3.6 aliases. Accept `chat_template_kwargs` in `/v1/chat/completions` request body; forward to tokenizer. Fixes multi-turn tool loop empty-args bug (pi-mono #3325 + QwenLM/Qwen3.6 #131). Regression fixture as `tests/agent_integration/test_qwen36_preserve_thinking.py`. Effort: ~1 day.
- [ ] **Qwen 3.6 · Streaming reasoning parser fix.** With `--reasoning-parser qwen3` + `enable_thinking:false`, deltas land in `delta.reasoning` instead of `delta.content`. Effort: ~0.5 day.
- [x] ~~**Qwen 3.6 · Pin tool parser per-alias.**~~ NOT NEEDED — HF-verified in 0.10.0 PR #1014 that Qwen 3.6 27B AND 35B chat templates both emit `<tool_call>\n<function=NAME>` XML format. The current `qwen3_coder_xml` parser is CORRECT for all Qwen 3.6 aliases (chat + coder). Qwen 3.6 unified their tool format on XML; the SGLang #25242 concern doesn't apply.
- [ ] **Gemma 4 · `<|channel|>thought` tool-template leak audit.** Community fix at `asf0/gemma4_jinja`. Test our `gemma4` tool parser against every leak case. Effort: ~1-2 days.
- [ ] **Gemma 4 · Codex #447 hang workaround.** `gemma-4-12b` hangs Codex ~60% on tool-use due to `thought\n…` degenerate loop. Ship best of: (a) tighter stop tokens via anthropic adapter, (b) repetition-penalty guard on `thought` tokens, or (c) documented prompt-side workaround. Effort: ~2 days (fix) or ~0.5 day (doc).
- [ ] **gpt-oss · Harmony `analysis`-channel leak regression fixture.** Continue #8990 has the exact repro. Add as `tests/agent_integration/test_gpt_oss_analysis_leak.py`. Also add Aider streaming test. Effort: ~1 day.

**Documentation (~1 day)**:

- [ ] **docs.rapidmlx.com blog post**: "Run Qwen 3.6 with qwen-code on your Mac, fully local" — capitalizes on Alibaba/Claude Code narrative
- [ ] **docs/agents/{codex,claude-code,opencode,qwen-code,openhands}.mdx** — one page per Tier-1 with copy-paste setup for each of the 3 families

### Confirmed integration tasks (NOT dependent on research)

These are known bugs / gaps that ship in 0.10.1 regardless of which agents make Tier 1:

- [ ] **Qwen 3.6 · Default `preserve_thinking=true`** for Qwen 3.6 aliases. Accept `chat_template_kwargs` in `/v1/chat/completions` request body; forward to tokenizer. Fixes multi-turn tool loop empty-args bug (pi-mono #3325 + QwenLM/Qwen3.6 #131). Regression fixture as `tests/agent_integration/test_qwen36_preserve_thinking.py`. Effort: ~1 day.
- [ ] **Qwen 3.6 · Streaming reasoning parser fix.** With `--reasoning-parser qwen3` + `enable_thinking:false`, deltas land in `delta.reasoning` instead of `delta.content`. Effort: ~0.5 day.
- [x] ~~**Qwen 3.6 · Pin tool parser per-alias.**~~ NOT NEEDED — HF-verified in 0.10.0 PR #1014 that Qwen 3.6 27B AND 35B chat templates both emit `<tool_call>\n<function=NAME>` XML format. The current `qwen3_coder_xml` parser is CORRECT for all Qwen 3.6 aliases (chat + coder). Qwen 3.6 unified their tool format on XML; the SGLang #25242 concern doesn't apply.
- [ ] **Gemma 4 · `<|channel|>thought` tool-template leak audit.** Community fix at `asf0/gemma4_jinja`. Test our `gemma4` tool parser against every leak case. Effort: ~1-2 days.
- [ ] **Gemma 4 · Codex #447 hang workaround.** `gemma-4-12b` hangs Codex ~60% on tool-use due to `thought\n…` degenerate loop. Ship best of: (a) tighter stop tokens via anthropic adapter, (b) repetition-penalty guard on `thought` tokens, or (c) documented prompt-side workaround. Effort: ~2 days (fix) or ~0.5 day (doc).
- [ ] **gpt-oss · Harmony `analysis`-channel leak regression fixture.** Continue #8990 has the exact repro. Add as `tests/agent_integration/test_gpt_oss_analysis_leak.py`. Also add Aider streaming test — same failure mode latent. Effort: ~1 day.

---

## 0.10.2 Release acceptance — Gemma 4 perf focus

Layer B for the family where our shipped 0.9.13 baseline has the largest gap. KV-sharing is the biggest single perf win of the entire 0.10 cycle.

### The five items (SOTA-gate-approved, finalized 2026-07-04)

- [ ] **[P0 · Tier A1] Gemma-3n cross-layer KV sharing loader plumb.** Middle-block K/V shared to all top layers per Gemma 3n architecture → **2× prefill / TTFT** (Google-benchmarked Gemma-4 vs Gemma-3-4B). Lossless (weight-declared). Blocker today: mlx-lm's KV dataclass allocates per-layer and silently duplicates the middle-block state. Fix is pure Python via `make_prompt_cache` borrow-mode; no Metal kernel work needed. This is the **highest ROI single item in the entire 0.10 cycle** — do this first. Effort: ~1 week. **Paper references**: Google Gemma-3 report (arxiv 2503.19786) + KV-sharing systematic study (arxiv 2410.14442) + Gemma 3n developer guide.
- [ ] **[P0 · Tier A / family pick] Google MTP assistant sidecars for E2B / E4B / 26B-A4B / 31B.** Extend `vllm_mlx/spec_decode/mtp/gemma4_inject.py` to each size's drafter shape (4 layers × per-size hidden). Load `-assistant-bf16` weights via existing sidecar path. Bench each vs baseline: expect +25-30% structured tok/s matching the 12B result. Effort: ~1 week. **Production reference**: Ollama v0.23.1 + llama.cpp both ship this — we're catching up. `research_ollama_gemma4_mtp.md` in knowledge memory has the K controller notes.
- [ ] **[P1 · Tier A2] Fused qSDPA + RoPE + RMSNorm Metal kernel (mlx-qsdpa integration).** Per mlx-qsdpa (Thump604): **1.7× decode vs mlx-lm baseline + 4× KV mem via inline dequant**. Lossless. Partial land in mlx-lm HEAD; ZMLX / mlx-metal-kernels are incubators for the rest. Wiring this in also unblocks the deferred TurboQuant K4V4 (Tier B2). Effort: ~1-2 weeks. **Paper reference**: mlx-qsdpa / mlx-metal-kernels repo READMEs; upstream mlx-lm PR tracking the split-fusion.
- [ ] **[P1 · Tier B2, gated on A2] TurboQuant / PolarQuant K4V4 KV cache.** arxiv 2504.19874 — 6× mem, 8× attention on H100, data-oblivious, ppl-neutral. Blocker: mlx-lm draft PR #1059 is **0.5× vs FP16 without fused Metal dequant-matmul** (same kernel scaffolding as A2). **Ship in the same PR pair as A2 or roll to 0.10.4 if A2 slips.** Do NOT ship without the fused kernel — will regress default users.
- [ ] **[P2 · defensive] PLE quantization probe test.** Early mlx-community/unsloth 4-bit variants quantized Per-Layer-Embedding tables → garbage output. Current mlx-community re-quants keep PLE in bf16 (probe verifies). Add to acceptance dashboard so bug can't recur silently. Effort: ~0.5 day. **Note**: this is a regression guard, not a perf technique — but it's cheap and matches the family focus.

### Perf-technique SOTA gate — Gemma 4 verdict

All five items above cleared the gate (A1 = paper + Google prod + lossless; A2 = incubator kernel + measured 1.7× on Gemma 4; MTP sidecar family = Ollama/llama.cpp prod + Gemma 4 12B already lossless in 0.9.13; B2 = paper + gated on A2; PLE probe = regression guard, not a claim). Zero Tier-C adhoc items in this list.

**Rejected during synthesis** (would have naively fit here but flunked the gate):
- KV eviction (H2O / SnapKV / StreamingLLM sinks) — approximate, fails quality gate (H2O collapses to 38% RULER, SnapKV misses 90% NIAH at claimed compression, StreamingLLM forgets middle content by design)
- Expert prefetch family (ExpertFlow / Fiddler / MoE-Infinity / PowerInfer-2 / Pregated MoE) — all solve CPU→GPU PCIe copy which doesn't exist on Apple Silicon UMA
- Continuous batching / PagedAttention / FlashInfer sparse prefill — multi-tenant tricks; rapid-mlx is single-user

---

## 0.10.3 Release acceptance — Qwen 3.6 perf focus + cross-family plumbing

### Qwen 3.6 Layer B (~3 weeks, SOTA-gate-approved 2026-07-04)

- [ ] **[P0 · family pick 1 · Tier A3] FastMTP head (arxiv 2509.18362).** Position-shared MTP head + self-distillation training. **2.03× vs NTP, +82% over vanilla MTP**; rejection-sampled lossless. Directly attacks the acceptance-rate decay at deeper K that our EV auto-K controller currently caps against. vLLM speculators RFC #267 has the reference impl. Train a FastMTP head against Qwen 3.6-27B + 35B-A3B; port loader into `qwen3_6_inject.py`. Effort: ~1-1.5 weeks. **This is A3 taking priority over the "wire the native head as-is" plan** — same infra, more speedup.
- [ ] **[P0] Wire native MTP for 27B + 35B-A3B (baseline path).** Create `vllm_mlx/spec_decode/mtp/qwen3_6_inject.py`, loads MTP head from the same checkpoint (not from a separate sidecar). Register in `spec_decode/mtp/dispatch.py:48-81` with model_type `qwen3_6` / `qwen3_6_moe`. Add to detection allowlist `spec_decode/mtp/detect.py:94-104`. Auto-K controller inherits from 0.9.13. **Fix the profile lie**: `Spec decode: ✗ disabled (no MTP/drafter trained)` → `✓ supported (native MTP head)`. This lands FIRST (baseline 2.24× via MTPLX numbers) → FastMTP layers on top after. Effort: ~1 week. **Production reference**: MTPLX repo (2.24× 27B).
- [ ] **[P0 · family pick 2 · Tier B3] DeltaNet `gated_delta_step` fp32 state fix.** Reference: mlx-lm #482 + ollama #15865. One-line Metal-kernel dtype change: state accumulator currently fp16, causes silent Qwen 3.6 long-context corruption > 128K tokens. **Quality gate, not perf — ship anyway** (SOTA gate makes an explicit exception for quality-preservation fixes). Effort: ~1 day (change + long-context regression test on Coder-Next 30B-A3B, 256K prompt).
- [ ] **[P1] DeltaNet path verify post-B3.** Bench 128K+ prefill on Coder-Next 30B-A3B on M4 Max — confirm no softmax fallback via `mlx-vlm ≥ 0.4.4` DeltaNet kernel path (was Peng et al. 2024 linear-attention). Effort: ~2 days.
- [ ] **[P1] TurboQuant K8V4 default-on for 27B / 35B aliases.** Profile already says `k8v4_verified` per audit — just flip flag. Effort: ~1 day.

### Cross-family plumbing (Phase 4, ~1 week)

- [ ] **[P0 · Tier A5] Always-on `mx.compile` with bucketed static shapes.** MLX docs + issue #2864 (0.30 regression window, fixed in 0.31). Fuses ~120 dispatches/block into few command buffers. **73% wall-time cut at batch=1 ctx=128; 2.7× vs PyTorch int8.** Bucket to 256 / 512 / 1024 / 2048 with LRU compiled-kernel cache. Hygiene item — every other Tier A win compounds on top. Applies to ALL 3 families, so lands in Phase 4 rather than per-family. Effort: ~3-4 days (buckets + cache + regression tests to catch #2864-style Metal driver breakage).
- [ ] **[P1] Suffix-decode bench sweep.** Fill in profile `suffix_decoding_tier` field from "unknown" → actual value for all 3 families. Run `scripts/bench_suffix_decode.py`. Effort: ~2-3 days.
- [ ] **[P1] Throttle profile fills.** Same, for `pflash_tier`, `turboquant_tier` where "unknown". Focus: Gemma 4 (all "unknown"), gpt-oss (all "unknown"). Effort: ~1-2 days.
- [ ] **[P1] Family × agent integration matrix doc.** New page at `docs/agents/matrix.mdx` cross-referencing 3 families × Tier-1 agent list from 0.10.1 research. Cell = "verified" / "known-issue-#N" / "untested". Effort: ~1 day.
- [ ] **[P2] `rapid-mlx info` gain "MTP status" row.** `MTP path: native / sidecar=<repo> / disabled` + `KV-share: yes/no` (Gemma 4 specific) + `mx.compile: bucketed / off`. Effort: ~2 hours.

### Optional 0.10.4 — gpt-oss perf focus (candidate, not committed)

Research surfaced two SOTA items that would give gpt-oss the same Layer-B treatment Gemma 4 and Qwen 3.6 get. **Not scheduled** — operator explicitly gated 0.10.2 to Gemma 4 and 0.10.3 to Qwen 3.6, and adding a fourth patch may drift the cycle. Listed here so we can promote if the 0.10.3 velocity is healthy, or roll to 0.11 otherwise.

- [Candidate] **Fused grouped-GEMM MoE dispatch** — mlx issue #3393; mlxcel v0.3 reference. Kills per-expert launch overhead. Lossless. Direct fit for gpt-oss (only paper-backed MoE dispatch optimization that doesn't rely on nonexistent PCIe copy). Effort: ~2 weeks.
- [Candidate] **Speculative MoE with distilled drafter** (arxiv 2503.04398). Small drafter + batched MoE verify amortizes dispatch **twice** — accept-rate speedup + per-launch expert amortization. Lossless. Closes the MTP-shaped gap since gpt-oss has no native MTP head. Effort: ~2-3 weeks (train drafter + integration).

---

## Perf technique SOTA gate — the standing rules

Every Layer B item must satisfy ALL of the following before shipping:

1. **Paper-backed** — arxiv ID or major-lab (Google / Meta / MS / OpenAI / Anthropic / Apple / Alibaba / DeepSeek) publication. "Some Discord post" doesn't count.
2. **Verified quality preservation** — either provably lossless-in-distribution (e.g., MTP, EAGLE-3 with acceptance verification) or measurable-and-acceptable quality delta (e.g., int8 W8A8 with < 2 point AIME25 regression as documented gate). No "seems fine" without a bench row.
3. **Production-deployed at scale** — vLLM / TensorRT-LLM / Ollama / mlx-lm / Google Vertex / OpenAI production shipping it counts. Discord-only doesn't.
4. **Fit for our chain-decode / MTP-adjacent workload** — tree-attention experiments that would require rewriting our batched engine are out-of-scope for 0.10.
5. **Applicable to at least one of the three target families** — no generic optimizations that don't move any hero alias.

### Adhoc / "民科" REJECT list (a running list — extend as we encounter them)

- Random temperature / top-p tuning sold as "perf" — that's quality, not speed
- Community quants without paper backing (e.g. "special calibration set")
- Layer-skipping / early-exit heuristics without formal treatment
- "Found on Twitter" claims of Nx speedup that no one has reproduced
- Prompt engineering that reduces token count as "throughput improvement"
- "This one weird sampler" — MinP, DRY, XTC etc. as *perf* claims (they're quality)

### The definitive tier list (finalized 2026-07-04 from research `ab84a1c5a52ab2fe2`)

**Tier A — SOTA + systematic + applicable in 0.10** (5 techniques, all folded into 0.10.2 / 0.10.3 / Phase 4 above):

| ID | Technique | Paper / reference | Verified speedup | Family fit |
|---|---|---|---|---|
| A1 | Gemma-3n cross-layer KV sharing | arxiv 2503.19786 + 2410.14442 + Google dev guide | **2× prefill/TTFT** | Gemma 4 |
| A2 | Fused qSDPA + RoPE + RMSNorm Metal kernel | mlx-qsdpa (Thump604); mlx-lm HEAD partial | **1.7× decode + 4× KV mem** | All 3 (Gemma 4 first user) |
| A3 | FastMTP (position-shared head + self-distill) | arxiv 2509.18362; vLLM speculators RFC #267 | **2.03× vs NTP, +82% over vanilla MTP** | Qwen 3.6 first user |
| A4 | Speculative MoE (small drafter + batched verify) | arxiv 2503.04398 | Accept-rate speedup + expert amortization | gpt-oss (candidate 0.10.4) |
| A5 | Always-on `mx.compile` + bucketed static shapes | MLX docs; issue #2864 (0.30 fixed in 0.31) | **73% wall-time cut at batch=1 ctx=128** | All 3 (Phase 4 plumbing) |

**Tier B — SOTA but blocked / higher risk** (revisit when triggers fire):

| ID | Technique | Blocker | Trigger to un-defer |
|---|---|---|---|
| B1 | EAGLE-3 (arxiv 2503.01840) | mlx-lm has prototype only (discussion #890), not merged | mlx-lm accepts EAGLE-3 PR |
| B2 | TurboQuant/PolarQuant KV (arxiv 2504.19874) | mlx-lm draft PR #1059 is 0.5× vs FP16 without fused Metal dequant-matmul | Ship together with A2 in 0.10.2, OR roll to 0.10.4 if A2 slips |
| B3 | DeltaNet fp32 state fix (mlx-lm #482, ollama #15865) | None — one-line Metal dtype change | **Ship anyway — quality gate** (folded into 0.10.3) |
| B4 | NPUMoE / ANE MoE offload (arxiv 2604.18788) | No integration path; ANE prefill previously killed | mlx-vlm exposes ANE MoE API |
| B5 | LMCache cross-session KV (arxiv 2510.09665) | No MLX port | Workload becomes many short sessions with shared system prompts |

**Tier C — REJECT (adhoc or non-fit)** — say no when someone proposes these:

| ID | Family of techniques | Why rejected |
|---|---|---|
| C1 | Expert-prefetch — ExpertFlow (2410.17954), MoE-Infinity, Fiddler (2402.07033), PowerInfer-2, Pregated MoE | All solve CPU→GPU PCIe copy which **doesn't exist on unified memory**. Their claimed 10× / 93% mem-save numbers evaporate on Apple Silicon. |
| C2 | Long-context KV eviction — H2O (2306.14048), SnapKV (2404.14469), StreamingLLM sinks (2309.17453) | Approximate + fails quality gate. H2O collapses to 38% RULER; SnapKV misses 90% NIAH at claimed compression; StreamingLLM forgets middle content by design. Chat-only, never long-context serving. |
| C3 | Continuous batching / PagedAttention / FlashInfer sparse prefill | Designed for multi-tenant concurrent-request servers. rapid-mlx is single-user; page-table indirection is pure overhead. Say no unless we pivot to multi-tenant. |

### Per-family top-2 (final)

| Family | Pick 1 | Pick 2 | Version |
|---|---|---|---|
| **Qwen 3.6** | FastMTP head (A3) | DeltaNet fp32 fix (B3) | 0.10.3 |
| **Gemma 4** | KV-sharing loader plumb (A1) | Google MTP sidecars all 5 sizes | 0.10.2 |
| **gpt-oss** | Fused grouped-GEMM MoE dispatch | Speculative MoE (A4) | 0.10.4 (candidate) or 0.11 |

---

## Don't do list — 0.10 additions to 0.9 baseline

Everything on 0.9-TODO's Don't-do list stays valid. New 0.10-specific entries:

| ❌ | Why |
|---|---|
| **DiffusionGemma MLX runtime path** | Google explicitly warns unified-memory Apple Silicon won't see the 4× speedup because diffusion is bandwidth-bound; needs 60GB+ full precision. `mlx-lm#1391` has no PR, `mlx-vlm#1355` reports mask memory blow-up. Keep alias, flag experimental, do not invest kernel work in 0.10. Revisit when mlx-lm loader lands. |
| **EAGLE-3 MLX port for gpt-oss in 0.10** | All published drafters are CUDA/vLLM-only. Weeks of greenfield MLX kernel work. ROI unclear vs Qwen native MTP (which is a checkpoint-in-hand delta, not a research spike). Deferred to 0.11 watchlist. |
| **SpinQuant / W8A8 / QuIP# in 0.10** | Moved to `0.11-TODO.md`. Do not sneak into 0.10 — user 体感 comes from top-tier family support, not from an invisible quant improvement. |
| ~~**Auto-detect tool parser for Qwen 3.6**~~ | VOID — HF-verified in PR #1014: Qwen 3.6 27B AND 35B chat both emit XML tool format via chat_template.jinja. `qwen3_coder_xml` is correct for the entire family. SGLang #25242 concern did not apply here. |
| **Bump mlx to drop `default_stream` shim** | 0.9-TODO documented this — mlx 0.31.x's `new_thread_local_stream` does NOT fix cross-thread eval. Shim is load-bearing. Do not propose removing on a "we bumped mlx" PR. |
| **Add 4B / 9B / 122B-A10B under Qwen 3.6 aliases** | Those are Qwen 3.5, not 3.6. Only 27B + 35B-A3B exist in the 3.6 family. Coder-Next is a separate line. |
| **Adhoc perf techniques** | See "Adhoc / 民科 REJECT list" above. |

---

## Deferred / watching

| Item | Why | Trigger to un-defer |
|---|---|---|
| gpt-oss-safeguard-120b MLX quant | Community hasn't quantized yet | mlx-community mirror appears |
| DeepSeek V4-Flash | Not published | Model card + weights on HF |
| Qwopus 4.x / Qwen 3.7 | Not published; Qwen 3.6 aliases already landed (11) so this watchlist has advanced once | Qwen team blog / HF release |
| DDTree-MLX Tmax-arch | Gated on training a ~1B Tmax drafter checkpoint | Public Tmax drafter release |
| EAGLE-3 MLX port | Weeks of greenfield kernel work; no upstream precedent | mlx-lm accepts EAGLE-3 PR |

---

## Source artifacts (research phase 2026-07-04)

- Qwen 3.6 externals: agent `a5b8f13daa7637a32` (17-source citation list; MTPLX repo + mlx-community MTP mirrors + pi-mono #3325 + SGLang #25242 + QwenLM/Qwen3.6 #131 + dasroot.net EAGLE bench)
- Gemma 4 externals: agent `ac0089d515d49d40a` (Google MTP blog + gemma4-benchmark repo + `mlx-vlm#1355` + asf0/gemma4_jinja + Agno #7493 + PLE quant discussion)
- gpt-oss externals: agent `a0b25205e6a830afb` (amazon P-EAGLE + RedHatAI EAGLE-3 + NVIDIA long-context EAGLE-3 + mlx-lm#367 MXFP4 kernel + Continue #8990 + pydantic-ai #2439 + siliconscore 120b bench)
- Internal audit: agent `aee996667ae9fcf30` (aliases.json + model_aliases.py:90-212 + spec_decode/mtp/dispatch.py:48-81 + spec_decode/mtp/detect.py:94-104 + agents/profiles/codex.yaml:72 + api/anthropic_adapter.py:110-240)
- **2026 agent framework landscape** — agent `aa64fe38e1d6d0575` — done, folded into § 0.10.1 above. Tier 1 = codex-cli + claude-code + opencode + qwen-code (new) + openhands. Archive hermes/goose. Rename langchain→langgraph. Add continue.dev + kilo-code Tier 2.
- **SOTA perf technique surface survey** — agent `ab84a1c5a52ab2fe2` — done, folded into § Perf technique SOTA gate + 0.10.2 + 0.10.3 + Phase 4 above. Tier A = A1 KV-share + A2 fused Metal + A3 FastMTP + A4 Spec-MoE (candidate) + A5 mx.compile-bucketed. Rejected: expert-prefetch family, KV eviction (H2O/SnapKV/sinks), multi-tenant batching primitives.
